#!/bin/sh
BRANCH_NAME=$(git rev-parse --abbrev-ref HEAD)
COMMIT_MSG=$(cat "$1")
if [[ $BRANCH_NAME =~ [^/]+$ ]]; then
	TEMP=${BASH_REMATCH[0]}
	if [[ $TEMP =~ ^[A-Z]+-[0-9]+ ]]; then
		echo "${BASH_REMATCH[0]} $COMMIT_MSG" > "$1"
	fi
else
    echo "$COMMIT_MSG" > "$1"
fi